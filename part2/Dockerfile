# Используем минимальный образ(image) Python
# Но можно например просто написать ubuntu:20.04(linux), да хоть windows, но такие образы содержат много лишнего
FROM python:3.9

# Устанавливаем рабочую директорию (создаем папку app)
WORKDIR /app

# Копируем файлы проекта с машины в докер
COPY . .

# Команда запуска приложения (будеи вызвана при создании контейнера)
CMD ["python", "server.py"]